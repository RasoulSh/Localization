namespace Localization.Base.Enums
{
    public enum LanguageAlphabetic
    {
        Latin = 0,          // Common Latin script
        Cyrillic = 1,       // Cyrillic script (used for Russian)
        Arabic = 2,         // Arabic script (used for Arabic)
        Chinese = 3,        // Chinese script (used for Chinese)
        Devanagari = 4,     // Devanagari script (used for Hindi)
        Hiragana = 5,       // Hiragana script (used for Japanese)
        Hangul = 6,         // Hangul script (used for Korean)
        Vietnamese = 7,     // Vietnamese script
        Thai = 8,           // Thai script
        Malay = 9,          // Malay script
        LatinRomanian = 10,  // Latin script with Romanian-specific characters (used for Romanian)
        ArabicPersian = 11   // Arabic script (used for Persian)
    }
}